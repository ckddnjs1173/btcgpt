# Deprecated — do not paste into Custom GPT

The Phase 16C–20 intelligence-context rules from this migration appendix are already included in:

`worker/openapi/GPT_INSTRUCTIONS.md`

Replace the GPT editor's entire Instructions field with the canonical file. Do not append this legacy file.
