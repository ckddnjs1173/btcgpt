# Optional relay upload configuration. Never commit real values.
BTC_RELAY_URL=https://REPLACE_WITH_WORKER.workers.dev
BTC_RELAY_UPLOAD_KEY=REPLACE_WITH_RANDOM_UPLOAD_KEY

# Binance credentials are entered in the app and protected with Electron
# safeStorage. Never place them in an environment file.
